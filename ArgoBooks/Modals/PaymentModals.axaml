<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:ArgoBooks.ViewModels"
             xmlns:controls="using:ArgoBooks.Controls"
             xmlns:behaviors="using:ArgoBooks.Behaviors"
             xmlns:local="using:ArgoBooks"
             xmlns:loc="using:ArgoBooks.Localization"
             xmlns:icons="using:ArgoBooks"
             mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="550"
             x:Class="ArgoBooks.Modals.PaymentModals"
             x:DataType="vm:PaymentModalsViewModel">

    <Design.DataContext>
        <vm:PaymentModalsViewModel />
    </Design.DataContext>

    <!-- All modals in one UserControl for full-screen coverage -->
    <Panel>
        <!-- Add / Edit Payment Modal -->
        <controls:ModalOverlay IsOpen="{Binding IsFormOpen, Mode=OneWay}"
                               ClosingCommand="{Binding RequestCloseFormCommand}">
            <controls:ModalOverlay.ModalContent>
                <Border Classes="modal-card"
                        Width="500"
                        MaxHeight="600">
                <Grid RowDefinitions="Auto,*,Auto">
                    <!-- Header -->
                    <controls:ModalHeader Grid.Row="0" Title="{Binding FormTitle}" CloseCommand="{Binding RequestCloseFormCommand}" />

                    <!-- Form Content -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="24" Spacing="16">
                            <!-- Portal payment info banner; IsPortalPayment is only set when editing -->
                            <Border Background="{DynamicResource SurfaceAltBrush}"
                                    CornerRadius="8"
                                    Padding="12"
                                    IsVisible="{Binding IsPortalPayment}">
                                <TextBlock Text="{loc:Loc 'This is an online payment. Only notes can be edited.'}"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap" />
                            </Border>

                            <!-- Invoice, hidden for portal payments -->
                            <StackPanel Spacing="6" IsVisible="{Binding !IsPortalPayment}">
                                <TextBlock Text="{loc:Loc Invoice}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                <controls:SearchableDropdown ItemsSource="{Binding InvoiceOptions}"
                                                            SelectedItem="{Binding SelectedInvoice}"
                                                            DisplayMemberPath="Display"
                                                            Placeholder="{loc:Loc 'Search for an invoice...'}"
                                                            ShowAddNew="False"
                                                            EmptyMessage="{loc:Loc 'No invoices found.'}"
                                                            EmptyCreateLinkText="{loc:Loc 'Create one'}"
                                                            EmptyCreateCommand="{Binding OpenCreateInvoiceCommand}" />
                                <TextBlock TextWrapping="Wrap" Text="{Binding ModalInvoiceError}"
                                           FontSize="12"
                                           Foreground="{DynamicResource ErrorBrush}">
                                    <TextBlock.IsVisible>
                                        <MultiBinding Converter="{x:Static BoolConverters.And}">
                                            <Binding Path="ModalInvoiceError" Converter="{x:Static StringConverters.IsNotNullOrEmpty}" />
                                            <Binding Path="!IsEditMode" />
                                        </MultiBinding>
                                    </TextBlock.IsVisible>
                                </TextBlock>
                            </StackPanel>

                            <!-- Amount, hidden for portal payments -->
                            <StackPanel Spacing="6" IsVisible="{Binding !IsPortalPayment}">
                                <TextBlock FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}">
                                    <Run Text="{loc:Loc Amount}" /><Run Text=" *" Foreground="{DynamicResource ErrorBrush}" />
                                </TextBlock>
                                <TextBox Text="{Binding ModalAmount, Mode=TwoWay}"
                                         Classes="form-input"
                                         PlaceholderText="0.00"
                                         behaviors:NumericInputBehavior.IsDecimalOnly="True"
                                         MaxLength="13" />
                                <TextBlock TextWrapping="Wrap" Text="{Binding ModalAmountError}"
                                           FontSize="12"
                                           Foreground="{DynamicResource ErrorBrush}"
                                           IsVisible="{Binding ModalAmountError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                            </StackPanel>

                            <!-- Date, hidden for portal payments -->
                            <StackPanel Spacing="6" IsVisible="{Binding !IsPortalPayment}">
                                <TextBlock FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}">
                                    <Run Text="{loc:Loc Date}" /><Run Text=" *" Foreground="{DynamicResource ErrorBrush}" />
                                </TextBlock>
                                <DatePicker SelectedDate="{Binding ModalDate}" Classes="form-datepicker" HorizontalAlignment="Stretch" />
                            </StackPanel>

                            <!-- Payment Method, hidden for portal payments -->
                            <StackPanel Spacing="6" IsVisible="{Binding !IsPortalPayment}">
                                <TextBlock FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}">
                                    <Run Text="{loc:Loc 'Payment Method'}" /><Run Text=" *" Foreground="{DynamicResource ErrorBrush}" />
                                </TextBlock>
                                <ComboBox ItemsSource="{Binding ModalPaymentMethodOptions}"
                                          SelectedItem="{Binding ModalPaymentMethod}"
                                          Classes="form-combobox">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource TranslateConverter}}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <!-- Reference Number, hidden for portal payments -->
                            <StackPanel Spacing="6" IsVisible="{Binding !IsPortalPayment}">
                                <TextBlock Text="{loc:Loc 'Reference Number'}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                <TextBox Text="{Binding ModalReferenceNumber, Mode=TwoWay}"
                                         Classes="form-input"
                                         PlaceholderText="{loc:Loc 'Check number, transaction ID, etc.'}"
                                         MaxLength="50" />
                            </StackPanel>

                            <!-- Notes -->
                            <StackPanel Spacing="6">
                                <TextBlock Text="{loc:Loc Notes}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                <TextBox Text="{Binding ModalNotes, Mode=TwoWay}"
                                         Classes="form-input"
                                         PlaceholderText="{loc:Loc 'Enter notes'}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="80"
                                         MaxHeight="120"
                                         MaxLength="500" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Footer -->
                    <Border Classes="modal-footer" Grid.Row="2">
                        <StackPanel Classes="modal-actions">
                            <Button Classes="secondary-button" Content="{loc:Loc Cancel}" Command="{Binding RequestCloseFormCommand}" />
                            <Button Classes="primary-button" Content="{Binding FormSaveText}" Command="{Binding SaveFormCommand}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
            </controls:ModalOverlay.ModalContent>
        </controls:ModalOverlay>
    </Panel>

</UserControl>
