<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:ArgoBooks.ViewModels"
             xmlns:loc="using:ArgoBooks.Localization"
             xmlns:controls="using:ArgoBooks.Controls"
             xmlns:local="using:ArgoBooks"
             mc:Ignorable="d" d:DesignWidth="420" d:DesignHeight="350"
             x:Class="ArgoBooks.Modals.CheckForUpdateModal"
             x:DataType="vm:CheckForUpdateModalViewModel">

    <Design.DataContext>
        <vm:CheckForUpdateModalViewModel />
    </Design.DataContext>

    <controls:ModalOverlay IsOpen="{Binding IsOpen}">
        <controls:ModalOverlay.ModalContent>
            <!-- Modal Container -->
            <Border Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Width="420"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BoxShadow="0 8 32 0 #40000000">

            <StackPanel>
                <!-- Header -->
                <Border Padding="24,20"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Grid.Column="0"
                                   Text="{loc:Loc 'Check for Updates'}"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}" />
                        <Button Grid.Column="1"
                                Classes="icon-button"
                                Command="{Binding CloseCommand}">
                            <PathIcon Data="{x:Static local:Icons.Close}"
                                      Width="20" Height="20" />
                        </Button>
                    </Grid>
                </Border>

                <!-- Content -->
                <StackPanel Margin="24" Spacing="24">
                    <!-- App Icon and Version -->
                    <StackPanel HorizontalAlignment="Center" Spacing="12">
                        <Border Width="64" Height="64"
                                CornerRadius="16"
                                HorizontalAlignment="Center"
                                ClipToBounds="True">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                                    <GradientStop Color="{DynamicResource PrimaryColor}" Offset="0" />
                                    <GradientStop Color="{DynamicResource AccentColor}" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="AB"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="{loc:Loc 'Argo Books'}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="{Binding CurrentVersion}"
                                   FontSize="13"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>

                    <!-- Status -->
                    <Border Background="{DynamicResource SurfaceAltBrush}"
                            CornerRadius="8"
                            Padding="16"
                            MinHeight="120">
                        <Panel VerticalAlignment="Center">
                            <!-- Checking State -->
                            <StackPanel IsVisible="{Binding IsChecking}"
                                        Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        Spacing="12">
                                <PathIcon Data="{x:Static local:Icons.Refresh}"
                                          Width="20" Height="20"
                                          Foreground="{DynamicResource PrimaryBrush}"
                                          Classes="spinning"
                                          RenderTransformOrigin="50%,50%">
                                    <PathIcon.RenderTransform>
                                        <RotateTransform />
                                    </PathIcon.RenderTransform>
                                </PathIcon>
                                <TextBlock Text="{loc:Loc 'Checking for updates...'}"
                                           FontSize="14"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" />
                            </StackPanel>

                            <!-- Up to Date State -->
                            <StackPanel IsVisible="{Binding IsUpToDate}"
                                        HorizontalAlignment="Center"
                                        Spacing="8">
                                <Border Width="40" Height="40"
                                        CornerRadius="20"
                                        Background="#10B981"
                                        HorizontalAlignment="Center">
                                    <PathIcon Data="{x:Static local:Icons.Check}"
                                              Width="20" Height="20"
                                              Foreground="White" />
                                </Border>
                                <TextBlock Text="{loc:Loc 'You''re up to date!'}"
                                           FontSize="14"
                                           FontWeight="Medium"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center" />
                                <TextBlock Text="{Binding LastChecked, StringFormat='Last checked: {0}'}"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextTertiaryBrush}"
                                           HorizontalAlignment="Center" />
                            </StackPanel>

                            <!-- Update Available State -->
                            <StackPanel IsVisible="{Binding HasUpdate}"
                                        HorizontalAlignment="Center"
                                        Spacing="12">
                                <Border Width="40" Height="40"
                                        CornerRadius="20"
                                        Background="{DynamicResource PrimaryBrush}"
                                        HorizontalAlignment="Center">
                                    <PathIcon Data="{x:Static local:Icons.Refresh}"
                                              Width="20" Height="20"
                                              Foreground="White" />
                                </Border>
                                <StackPanel HorizontalAlignment="Center" Spacing="4">
                                    <TextBlock Text="{loc:Loc 'Update Available!'}"
                                               FontSize="14"
                                               FontWeight="Medium"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               HorizontalAlignment="Center" />
                                    <TextBlock Text="{Binding NewVersion, StringFormat='{}{0} is now available'}"
                                               FontSize="12"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               HorizontalAlignment="Center" />
                                </StackPanel>
                            </StackPanel>

                            <!-- Downloading State -->
                            <StackPanel IsVisible="{Binding IsDownloading}"
                                        HorizontalAlignment="Center"
                                        Spacing="12">
                                <Border Width="40" Height="40"
                                        CornerRadius="20"
                                        Background="{DynamicResource PrimaryBrush}"
                                        HorizontalAlignment="Center">
                                    <PathIcon Data="{x:Static local:Icons.Download}"
                                              Width="20" Height="20"
                                              Foreground="White" />
                                </Border>
                                <StackPanel HorizontalAlignment="Center" Spacing="4">
                                    <TextBlock Text="{loc:Loc 'Downloading update...'}"
                                               FontSize="14"
                                               FontWeight="Medium"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               HorizontalAlignment="Center" />
                                    <TextBlock Text="{Binding DownloadProgress, StringFormat='{}{0}%'}"
                                               FontSize="12"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               HorizontalAlignment="Center" />
                                </StackPanel>
                                <ProgressBar Minimum="0" Maximum="100"
                                             Value="{Binding DownloadProgress}"
                                             Height="6"
                                             CornerRadius="3"
                                             Width="280"
                                             Foreground="{DynamicResource PrimaryBrush}" />
                            </StackPanel>

                            <!-- Ready to Install State -->
                            <StackPanel IsVisible="{Binding IsReadyToInstall}"
                                        HorizontalAlignment="Center"
                                        Spacing="12">
                                <Border Width="40" Height="40"
                                        CornerRadius="20"
                                        Background="#10B981"
                                        HorizontalAlignment="Center">
                                    <PathIcon Data="{x:Static local:Icons.Check}"
                                              Width="20" Height="20"
                                              Foreground="White" />
                                </Border>
                                <TextBlock Text="{loc:Loc 'Update downloaded successfully!'}"
                                           FontSize="14"
                                           FontWeight="Medium"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center" />
                                <Button Classes="primary-button"
                                        Content="{loc:Loc 'Restart to Apply Update'}"
                                        Command="{Binding ApplyUpdateCommand}"
                                        HorizontalAlignment="Center"
                                        Margin="0,4,0,0"
                                        Background="#10B981"
                                        Foreground="White" />
                            </StackPanel>

                            <!-- Error State -->
                            <StackPanel IsVisible="{Binding HasError}"
                                        HorizontalAlignment="Center"
                                        Spacing="8">
                                <Border Width="40" Height="40"
                                        CornerRadius="20"
                                        Background="{DynamicResource ErrorBrush}"
                                        HorizontalAlignment="Center">
                                    <PathIcon Data="{x:Static local:Icons.Info}"
                                              Width="20" Height="20"
                                              Foreground="White" />
                                </Border>
                                <TextBlock Text="{loc:Loc 'Something went wrong'}"
                                           FontSize="14"
                                           FontWeight="Medium"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           HorizontalAlignment="Center" />
                                <SelectableTextBlock Text="{Binding ErrorMessage}"
                                                    FontSize="12"
                                                    Foreground="{DynamicResource TextTertiaryBrush}"
                                                    HorizontalAlignment="Center"
                                                    TextWrapping="Wrap"
                                                    MaxWidth="340" />
                                <Button Classes="secondary-button"
                                        Content="{loc:Loc 'Try Again'}"
                                        Command="{Binding CheckForUpdatesCommand}"
                                        HorizontalAlignment="Center"
                                        Margin="0,4,0,0" />
                            </StackPanel>
                        </Panel>
                    </Border>
                </StackPanel>

                <!-- Footer -->
                <Border Padding="24,16"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,1,0,0">
                    <Button Classes="link-button"
                            Content="{loc:Loc 'View Release Notes'}"
                            Command="{Binding ViewReleaseNotesCommand}"
                            HorizontalAlignment="Center" />
                </Border>
            </StackPanel>
            </Border>
        </controls:ModalOverlay.ModalContent>
    </controls:ModalOverlay>

    <UserControl.Styles>
        <!-- Spinning Animation -->
        <Style Selector="PathIcon.spinning">
            <Style.Animations>
                <Animation Duration="0:0:1" IterationCount="INFINITE">
                    <KeyFrame Cue="0%">
                        <Setter Property="RotateTransform.Angle" Value="0" />
                    </KeyFrame>
                    <KeyFrame Cue="100%">
                        <Setter Property="RotateTransform.Angle" Value="360" />
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
    </UserControl.Styles>
</UserControl>
