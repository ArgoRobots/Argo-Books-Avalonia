<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:ArgoBooks.ViewModels"
             xmlns:controls="using:ArgoBooks.Controls"
             xmlns:behaviors="using:ArgoBooks.Behaviors"
             xmlns:loc="using:ArgoBooks.Localization"
             xmlns:local="using:ArgoBooks"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ArgoBooks.Modals.StockLevelsModals"
             x:DataType="vm:StockLevelsModalsViewModel">

    <Panel>
        <!-- Adjust Stock Modal -->
        <controls:ModalOverlay IsOpen="{Binding IsAdjustStockModalOpen}"
                               ClosingCommand="{Binding RequestCloseAdjustStockModalCommand}">
            <controls:ModalOverlay.ModalContent>
                <Border Classes="modal-card"
                    Width="550">
                <Grid RowDefinitions="Auto,*,Auto">
                    <!-- Modal Header -->
                    <controls:ModalHeader Grid.Row="0"
                                          Title="{loc:Loc 'Adjust Stock'}"
                                          Subtitle="{Binding SelectedItemProductName}"
                                          CloseCommand="{Binding RequestCloseAdjustStockModalCommand}" />

                    <!-- Modal Content -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="24" Spacing="16">
                            <!-- Adjustment Type -->
                            <StackPanel Spacing="6">
                                <TextBlock FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}">
                                    <Run Text="{loc:Loc 'Adjustment Type'}" /><Run Text=" *" Foreground="{DynamicResource ErrorBrush}" />
                                </TextBlock>
                                <ComboBox ItemsSource="{Binding AdjustmentTypes}"
                                          SelectedItem="{Binding AdjustmentType}"
                                          Classes="form-combobox"
                                          HorizontalAlignment="Stretch">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource TranslateConverter}}" />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>

                            <!-- Stock Values Row -->
                            <Grid ColumnDefinitions="*,16,*,16,*">
                                <!-- Current Stock -->
                                <StackPanel Grid.Column="0" Spacing="6">
                                    <TextBlock Text="{loc:Loc 'Current Stock'}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <Border Background="{DynamicResource SurfaceAltBrush}"
                                            CornerRadius="6"
                                            Padding="12,10"
                                            MinHeight="{StaticResource FormControlHeight}">
                                        <TextBlock Text="{Binding CurrentStockText}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                </StackPanel>

                                <!-- Adjustment Quantity -->
                                <StackPanel Grid.Column="2" Spacing="6">
                                    <TextBlock FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}">
                                        <Run Text="{loc:Loc Quantity}" /><Run Text=" *" Foreground="{DynamicResource ErrorBrush}" />
                                    </TextBlock>
                                    <TextBox Text="{Binding AdjustmentQuantity, Mode=TwoWay}"
                                             Classes="form-input"
                                             PlaceholderText="{loc:Loc 'Enter quantity'}"
                                             behaviors:NumericInputBehavior.IsDecimalOnly="True"
                                             MaxLength="7" />
                                    <TextBlock Text="{loc:Loc 'Please enter a valid quantity.'}"
                                               FontSize="12"
                                               Foreground="{DynamicResource ErrorBrush}"
                                               IsVisible="{Binding HasAdjustmentQuantityError}" />
                                </StackPanel>

                                <!-- New Stock Level -->
                                <StackPanel Grid.Column="4" Spacing="6">
                                    <TextBlock Text="{loc:Loc 'New Stock'}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <Border Background="{DynamicResource SurfaceAltBrush}"
                                            CornerRadius="6"
                                            Padding="12,10"
                                            MinHeight="{StaticResource FormControlHeight}">
                                        <TextBlock Text="{Binding CalculatedNewStock}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                </StackPanel>
                            </Grid>

                            <!-- Reason/Notes -->
                            <StackPanel Spacing="6">
                                <TextBlock Text="{loc:Loc 'Reason/Notes'}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                <TextBox Text="{Binding AdjustmentReason, Mode=TwoWay}"
                                         Classes="form-input"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="80"
                                         MaxHeight="120"
                                         PlaceholderText="{loc:Loc 'Enter reason for adjustment...'}"
                                         MaxLength="500" />
                            </StackPanel>

                            <!-- Error Message -->
                            <TextBlock TextWrapping="Wrap" Text="{Binding AdjustmentError}"
                                       FontSize="12"
                                       Foreground="{DynamicResource ErrorBrush}"
                                       IsVisible="{Binding AdjustmentError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Modal Footer -->
                    <Border Classes="modal-footer" Grid.Row="2">
                        <StackPanel Classes="modal-actions">
                            <Button Classes="secondary-button" Content="{loc:Loc Cancel}" Command="{Binding CloseAdjustStockModalCommand}" />
                            <Button Classes="primary-button" Content="{loc:Loc 'Apply Adjustment'}" Command="{Binding SaveAdjustmentCommand}" />
                        </StackPanel>
                    </Border>
                </Grid>
                </Border>
            </controls:ModalOverlay.ModalContent>
        </controls:ModalOverlay>

        <!-- Transfer Stock Modal -->
        <controls:ModalOverlay IsOpen="{Binding IsTransferStockModalOpen}"
                               ClosingCommand="{Binding CloseTransferStockModalCommand}">
            <controls:ModalOverlay.ModalContent>
                <Border Classes="modal-card"
                    Width="550">
                <Grid RowDefinitions="Auto,*,Auto">
                    <controls:ModalHeader Grid.Row="0"
                                          Title="{loc:Loc 'Transfer Stock'}"
                                          Subtitle="{Binding TransferProductName}"
                                          CloseCommand="{Binding CloseTransferStockModalCommand}" />

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="24" Spacing="16">
                            <Grid ColumnDefinitions="*,16,*">
                                <StackPanel Grid.Column="0" Spacing="6">
                                    <TextBlock Text="{loc:Loc From}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <Border Background="{DynamicResource SurfaceAltBrush}"
                                            CornerRadius="6"
                                            Padding="12,10"
                                            MinHeight="{StaticResource FormControlHeight}">
                                        <TextBlock Text="{Binding TransferFromLocationName}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Spacing="6">
                                    <TextBlock Text="{loc:Loc 'In Stock'}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <Border Background="{DynamicResource SurfaceAltBrush}"
                                            CornerRadius="6"
                                            Padding="12,10"
                                            MinHeight="{StaticResource FormControlHeight}">
                                        <TextBlock Text="{Binding TransferAvailableText}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                </StackPanel>
                            </Grid>

                            <Grid ColumnDefinitions="*,16,*">
                                <StackPanel Grid.Column="0" Spacing="6">
                                    <controls:SearchableDropdown
                                        Label="{loc:Loc To}"
                                        IsRequired="True"
                                        ItemsSource="{Binding TransferLocations}"
                                        SelectedItem="{Binding TransferToLocation}"
                                        DisplayMemberPath="Name"
                                        Placeholder="{loc:Loc 'Select a location...'}"
                                        ShowAddNew="False"
                                        EmptyMessage="{loc:Loc 'No other locations'}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Spacing="6">
                                    <TextBlock FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}">
                                        <Run Text="{loc:Loc Quantity}" /><Run Text=" *" Foreground="{DynamicResource ErrorBrush}" />
                                    </TextBlock>
                                    <TextBox Text="{Binding TransferQuantity, Mode=TwoWay}"
                                             Classes="form-input"
                                             PlaceholderText="{loc:Loc 'Enter quantity'}"
                                             behaviors:NumericInputBehavior.IsDecimalOnly="True"
                                             MaxLength="10" />
                                </StackPanel>
                            </Grid>

                            <StackPanel Spacing="6">
                                <TextBlock Text="{loc:Loc 'Reason/Notes'}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                <TextBox Text="{Binding TransferNotes, Mode=TwoWay}"
                                         Classes="form-input"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         MinHeight="80"
                                         MaxHeight="120"
                                         MaxLength="500" />
                            </StackPanel>

                            <TextBlock TextWrapping="Wrap" Text="{Binding TransferError}"
                                       FontSize="12"
                                       Foreground="{DynamicResource ErrorBrush}"
                                       IsVisible="{Binding TransferError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                        </StackPanel>
                    </ScrollViewer>

                    <Border Classes="modal-footer" Grid.Row="2">
                        <StackPanel Classes="modal-actions">
                            <Button Classes="secondary-button" Content="{loc:Loc Cancel}" Command="{Binding CloseTransferStockModalCommand}" />
                            <Button Classes="primary-button" Content="{loc:Loc Transfer}" Command="{Binding SaveTransferCommand}" />
                        </StackPanel>
                    </Border>
                </Grid>
                </Border>
            </controls:ModalOverlay.ModalContent>
        </controls:ModalOverlay>

        <!-- Add Item Modal -->
        <controls:ModalOverlay IsOpen="{Binding IsAddItemModalOpen}"
                               ClosingCommand="{Binding RequestCloseAddItemModalCommand}">
            <controls:ModalOverlay.ModalContent>
                <Border Classes="modal-card"
                    Width="600">
                <Grid RowDefinitions="Auto,*,Auto">
                    <!-- Modal Header -->
                    <controls:ModalHeader Grid.Row="0"
                                          Title="{loc:Loc 'Add Inventory Item'}"
                                          CloseCommand="{Binding RequestCloseAddItemModalCommand}" />

                    <!-- Modal Content -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="24" Spacing="16">
                            <!-- Product Selection -->
                            <StackPanel Spacing="6">
                                <TextBlock FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}">
                                    <Run Text="{loc:Loc Product}" /><Run Text=" *" Foreground="{DynamicResource ErrorBrush}" />
                                </TextBlock>
                                <controls:SearchableDropdown ItemsSource="{Binding AvailableProducts}"
                                                            SelectedItem="{Binding SelectedProduct}"
                                                            DisplayMemberPath="Name"
                                                            SearchMemberPath="Id"
                                                            Placeholder="{loc:Loc 'Search products...'}"
                                                            ShowAddNew="False"
                                                            EmptyMessage="{loc:Loc 'No products available.'}"
                                                            EmptyCreateLinkText="{loc:Loc 'Create one'}"
                                                            EmptyCreateCommand="{Binding OpenCreateProductCommand}" />
                                <TextBlock TextWrapping="Wrap" Text="{Binding AddItemProductError}" FontSize="12" Foreground="{DynamicResource ErrorBrush}"
                                           IsVisible="{Binding AddItemProductError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                            </StackPanel>

                            <!-- SKU and Location -->
                            <Grid ColumnDefinitions="*,16,*">
                                <StackPanel Grid.Column="0" Spacing="6">
                                    <TextBlock Text="{loc:Loc SKU}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <TextBox Text="{Binding AddItemSku, Mode=TwoWay}"
                                             Classes="form-input"
                                             PlaceholderText="{loc:Loc 'Enter SKU (optional)'}"
                                             MaxLength="50" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" Spacing="6">
                                    <controls:SearchableDropdown
                                        Label="{loc:Loc Location}"
                                        IsRequired="True"
                                        ItemsSource="{Binding AvailableLocations}"
                                        SelectedItem="{Binding SelectedLocation}"
                                        DisplayMemberPath="Name"
                                        Placeholder="{loc:Loc 'Select a location...'}"
                                        HasError="{Binding HasLocationError}"
                                        ErrorMessage="{loc:Loc 'Please select a location'}"
                                        ShowAddNew="False"
                                        EmptyMessage="{loc:Loc 'No locations found'}"
                                        EmptyCreateLinkText="{loc:Loc 'Create one'}"
                                        EmptyCreateCommand="{Binding OpenCreateLocationCommand}" />
                                </StackPanel>
                            </Grid>

                            <!-- Initial Quantity and Reorder Point -->
                            <Grid ColumnDefinitions="*,16,*">
                                <StackPanel Grid.Column="0" Spacing="6">
                                    <TextBlock FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}">
                                        <Run Text="{loc:Loc 'Initial Quantity'}" /><Run Text=" *" Foreground="{DynamicResource ErrorBrush}" />
                                    </TextBlock>
                                    <TextBox Text="{Binding AddItemQuantity, Mode=TwoWay}"
                                             Classes="form-input"
                                             PlaceholderText="0"
                                             behaviors:NumericInputBehavior.IsDecimalOnly="True"
                                             MaxLength="7" />
                                    <TextBlock Text="{loc:Loc 'Please enter a valid quantity.'}"
                                               FontSize="12"
                                               Foreground="{DynamicResource ErrorBrush}"
                                               IsVisible="{Binding HasAddItemQuantityError}" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" Spacing="6">
                                    <TextBlock Text="{loc:Loc 'Reorder Point'}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                    <TextBox Text="{Binding AddItemReorderPoint, Mode=TwoWay}"
                                             Classes="form-input"
                                             PlaceholderText="10"
                                             behaviors:NumericInputBehavior.IsDecimalOnly="True"
                                             MaxLength="7" />
                                </StackPanel>
                            </Grid>

                            <!-- Overstock Threshold -->
                            <StackPanel Spacing="6">
                                <TextBlock Text="{loc:Loc 'Overstock Threshold'}" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextSecondaryBrush}" />
                                <TextBox Text="{Binding AddItemOverstockThreshold, Mode=TwoWay}"
                                         Classes="form-input"
                                         PlaceholderText="100"
                                         behaviors:NumericInputBehavior.IsDecimalOnly="True"
                                         MaxLength="7" />
                            </StackPanel>

                            <!-- Error Message -->
                            <TextBlock TextWrapping="Wrap" Text="{Binding AddItemError}"
                                       FontSize="12"
                                       Foreground="{DynamicResource ErrorBrush}"
                                       IsVisible="{Binding AddItemError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Modal Footer -->
                    <Border Classes="modal-footer" Grid.Row="2">
                        <StackPanel Classes="modal-actions">
                            <Button Classes="secondary-button" Content="{loc:Loc Cancel}" Command="{Binding CloseAddItemModalCommand}" />
                            <Button Classes="primary-button" Content="{loc:Loc 'Add Item'}" Command="{Binding SaveNewItemCommand}" />
                        </StackPanel>
                    </Border>
                </Grid>
                </Border>
            </controls:ModalOverlay.ModalContent>
        </controls:ModalOverlay>

        <!-- Filter Modal -->
        <controls:ModalOverlay IsOpen="{Binding IsFilterModalOpen}"
                               ClosingCommand="{Binding RequestCloseFilterModalCommand}">
            <controls:ModalOverlay.ModalContent>
                <Border Classes="modal-card"
                    Width="480">
                <StackPanel>
                    <!-- Modal Header -->
                    <controls:ModalHeader Title="{loc:Loc 'Filter Stock Levels'}" CloseCommand="{Binding RequestCloseFilterModalCommand}" />

                    <!-- Modal Content -->
                    <StackPanel Spacing="16" Margin="24">
                        <!-- Category -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="{loc:Loc Category}"
                                       FontSize="13"
                                       FontWeight="Medium"
                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                            <controls:SearchableDropdown ItemsSource="{Binding FilterCategories}"
                                                         SelectedItem="{Binding FilterCategory}"
                                                         Placeholder="{loc:Loc 'Search categories...'}" />
                        </StackPanel>

                        <!-- Location -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="{loc:Loc Location}"
                                       FontSize="13"
                                       FontWeight="Medium"
                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                            <controls:SearchableDropdown ItemsSource="{Binding FilterLocations}"
                                                         SelectedItem="{Binding FilterLocation}"
                                                         Placeholder="{loc:Loc 'Search locations...'}" />
                        </StackPanel>

                        <!-- Stock Status -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="{loc:Loc 'Stock Status'}"
                                       FontSize="13"
                                       FontWeight="Medium"
                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                            <ComboBox ItemsSource="{Binding FilterStatusOptions}"
                                      SelectedItem="{Binding FilterStatus}"
                                      Classes="form-combobox"
                                      HorizontalAlignment="Stretch">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource TranslateConverter}}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>

                    <!-- Modal Footer -->
                    <Border Classes="modal-footer">
                        <Grid ColumnDefinitions="Auto,*,Auto,Auto">
                            <Button Grid.Column="0"
                                    Content="{loc:Loc 'Clear filters'}"
                                    Classes="secondary-button"
                                    HorizontalAlignment="Left"
                                    Command="{Binding ClearFiltersCommand}" />
                            <Button Grid.Column="2"
                                    Content="{loc:Loc Cancel}"
                                    Classes="secondary-button"
                                    Margin="0,0,12,0"
                                    Command="{Binding RequestCloseFilterModalCommand}" />
                            <Button Grid.Column="3"
                                    Content="{loc:Loc 'Apply Filters'}"
                                    Classes="primary-button"
                                    Command="{Binding ApplyFiltersCommand}" />
                        </Grid>
                    </Border>
                </StackPanel>
                </Border>
            </controls:ModalOverlay.ModalContent>
        </controls:ModalOverlay>
    </Panel>
</UserControl>
